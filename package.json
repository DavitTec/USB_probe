{
  "name": "usb-probe",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "shellcheck scripts/*.sh && prettier --check .",
    "format": "prettier --write .",
    "test": "bash tests/test_usb_info.sh",
    "commit": "cz",
    "prepare": "husky",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s",
    "changelog:first": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0",
    "changelog:fix": "./scripts/changelog-fix.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davittec/usb-probe.git"
  },
  "keywords": [
    "usb",
    "probe",
    "usb-probe",
    "davittec"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.7.1",
  "devDependencies": {
    "commitizen": "^4.3.1",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^9.1.7",
    "prettier": "^3.6.2",
    "shellcheck": "^3.1.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
